{
  "name": "data_server",
  "version": "1.0.0",
  "description": "This project is made to keep the data for my projects using the json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json --port 5000"
  },
  "author": "notelyfe",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
